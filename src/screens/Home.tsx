import React from 'react';

const Home = () => (
  <div>
    <h2>Welcome to the VUI React App</h2>
    <p>Use voice commands to experience the App for all features.</p>
  </div>
);

export default Home;
